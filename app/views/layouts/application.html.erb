<!DOCTYPE html>
<html>
<head>
  <title>Filmes App</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
</head>
<body>

  <header style="background: #eee; padding: 10px; margin-bottom: 20px;">
    <%= link_to "Home (Filmes)", root_path %> | 
    
    <% if user_signed_in? %>
      Olá, <%= current_user.email %> |
      <%= link_to 'Novo Filme', new_movie_path %> |
      <%= link_to 'Editar Perfil', edit_user_registration_path %> |
      <%= button_to 'Sair (Logout)', destroy_user_session_path, method: :delete %>
    <% else %>
      <%= link_to 'Cadastre-se', new_user_registration_path %> |
      <%= link_to 'Entrar (Login)', new_user_session_path %>
    <% end %>
  </header>

  <p class="notice" style="color: green; font-weight: bold;"><%= notice %></p>
  <p class="alert" style="color: red; font-weight: bold;"><%= alert %></p>

  <div class="container">
    <%= yield %>
  </div>
  </body>
</html>


